# AI Asset Management Chatbot
# Streamlit Cloud compatible dependencies

# Core framework
streamlit==1.28.1

# LangChain ecosystem (avoid root 'langchain' to prevent conflicts)
# Align versions so that langsmith constraint matches langchain-core requirements
langchain-community>=0.1.0,<0.2.0
langchain-groq>=0.1.0,<0.2.0
langsmith>=0.1.0,<0.2.0

# Vector store and embeddings
faiss-cpu==1.12.0
sentence-transformers==2.2.2

# Data processing (use versions with pre-built wheels, Python 3.13 compatible)
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# Environment management
python-dotenv==1.0.0

# Additional dependencies for stability
pydantic>=2.0.0,<3.0.0
typing-extensions>=4.0.0
