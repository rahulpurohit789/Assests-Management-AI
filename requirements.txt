# AI Asset Management Chatbot
# Streamlit Cloud compatible dependencies

# Core framework (supports NumPy 2.x on Python 3.13)
streamlit>=1.36.0,<2.0.0

# LangChain ecosystem (avoid root 'langchain' to prevent conflicts)
# Align on langchain-core>=0.2 which requires langsmith>=0.1,<0.2
langchain-community>=0.2.0,<0.4.0
langchain-groq>=0.2.0,<0.3.0
langsmith>=0.1.0,<0.2.0

# Vector store and embeddings (CPU-only, no torch)
faiss-cpu==1.12.0
# FastEmbed compatible with Python 3.13 and ONNX Runtime 1.20+
fastembed==0.7.3
onnxruntime>=1.20.0,<1.21.0

# Data processing (use versions with pre-built wheels, Python 3.13 compatible)
pandas>=2.0.0,<3.0.0
numpy>=2.1.0,<3.0.0

# Environment management
python-dotenv==1.0.0

# Additional dependencies for stability
pydantic>=2.0.0,<3.0.0
typing-extensions>=4.0.0

# Streamlit dependency constraints
rich==13.9.4
